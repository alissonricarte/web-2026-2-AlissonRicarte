version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd ContaCerta
        - npm ci

    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: ContaCerta/dist
    files:
      - '**/*'

  cache:
    paths:
      - ContaCerta/node_modules/**/*
